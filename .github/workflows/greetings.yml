name: Greetings

on: [pull_request, issues]

jobs:
  greeting:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/first-interaction@v1
      with:
        repo-token: ${{ secrets.GITHUB_TOKEN }}
        issue-message: 'Welcome! Please remember to look over our guidelines.'' first issue'
        pr-message: 'Welcome! Please remember to look over our guidelines.'' first pr'
